# Copy to .env.local. NEXT_PUBLIC_* vars are exposed to the browser — never put secrets
# (API keys, DB credentials, ingest keys) in this file; those live in backend/.env only.

NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_WS_URL=ws://localhost:4000/ws
